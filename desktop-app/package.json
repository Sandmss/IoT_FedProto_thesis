{
  "name": "iot-fedproto-desktop",
  "version": "0.1.0",
  "description": "Electron + TypeScript desktop prototype for the IoT FedProto thesis project.",
  "main": "dist/electron/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "keywords": [
    "electron",
    "federated-learning",
    "typescript",
    "iot"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "electron": "^37.0.0",
    "typescript": "^5.8.0"
  }
}
